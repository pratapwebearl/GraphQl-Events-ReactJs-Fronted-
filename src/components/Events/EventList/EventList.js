import React from "react";

const eventList = props => (
    <ul className="events__list">
        {eventList}
        <li className="events__list-item">demoo</li>
    </ul>
);

export default eventList;